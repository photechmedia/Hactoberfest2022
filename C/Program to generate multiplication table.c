#include <stdio.h>
int main() {
  int num;
  printf("Enter an integer: ");
  scanf("%d", &num);
  for ( int i = 1; i <= 10; i++) {
    printf("the multiplication table of the entered number is \n :");
    printf("%d * %d = %d \n", n, i, n * i);
  }
  return 0;
}
